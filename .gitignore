# system
.DS_Store
Thumbs.db

# editors
.vscode/
.idea/
*.swp
*.swo

# node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# build outputs
dist/
build/
.next/
.nuxt/
out/

# deployment — Vercel manages its own git inside this folder.
# The project git tracks the prototype; Vercel owns deployment/.
deployment/

# env
.env
.env.local
.env.*.local

# raw video source — too big for GitHub (>100MB). Only the compressed
# intro.mp4 + poster.jpg are committed/deployed.
landing-page-video/*.mov
landing-page-video/*.MOV

# misc
*.log
.cache/
pre-contract-landing-page/
deployment
